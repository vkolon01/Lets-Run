export interface UserModel {
    email: string,
    username: string,
    avatar: string,
    firstName: string,
    lastName: string,
    createdAt: Date,
    dob: Date,

    // comment: [string],
    // likedEvent: [string],
    // createdEvent: [string],
    // blackList: [string],
    // friendList: [string]
}